/**
 * Secure API
 * Secure API
 *
 * The version of the OpenAPI document: 2.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SecurePaymentDetailsRequest model module.
 * @module model/SecurePaymentDetailsRequest
 * @version 2.3
 */
class SecurePaymentDetailsRequest {
    /**
     * Constructs a new <code>SecurePaymentDetailsRequest</code>.
     * @alias module:model/SecurePaymentDetailsRequest
     * @param type {module:model/SecurePaymentDetailsRequest.TypeEnum} 
     */
    constructor(type) { 
        
        SecurePaymentDetailsRequest.initialize(this, type);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, type) { 
        obj['type'] = type;
    }

    /**
     * Constructs a <code>SecurePaymentDetailsRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SecurePaymentDetailsRequest} obj Optional instance to populate.
     * @return {module:model/SecurePaymentDetailsRequest} The populated <code>SecurePaymentDetailsRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SecurePaymentDetailsRequest();

            if (data.hasOwnProperty('id')) {
                obj['id'] = ApiClient.convertToType(data['id'], 'String');
            }
            if (data.hasOwnProperty('paymentToken')) {
                obj['paymentToken'] = ApiClient.convertToType(data['paymentToken'], 'String');
            }
            if (data.hasOwnProperty('type')) {
                obj['type'] = ApiClient.convertToType(data['type'], 'String');
            }
            if (data.hasOwnProperty('includeRefundTransactions')) {
                obj['includeRefundTransactions'] = ApiClient.convertToType(data['includeRefundTransactions'], 'Boolean');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SecurePaymentDetailsRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SecurePaymentDetailsRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SecurePaymentDetailsRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['id'] && !(typeof data['id'] === 'string' || data['id'] instanceof String)) {
            throw new Error("Expected the field `id` to be a primitive type in the JSON string but got " + data['id']);
        }
        // ensure the json data is a string
        if (data['paymentToken'] && !(typeof data['paymentToken'] === 'string' || data['paymentToken'] instanceof String)) {
            throw new Error("Expected the field `paymentToken` to be a primitive type in the JSON string but got " + data['paymentToken']);
        }
        // ensure the json data is a string
        if (data['type'] && !(typeof data['type'] === 'string' || data['type'] instanceof String)) {
            throw new Error("Expected the field `type` to be a primitive type in the JSON string but got " + data['type']);
        }

        return true;
    }


}

SecurePaymentDetailsRequest.RequiredProperties = ["type"];

/**
 * @member {String} id
 */
SecurePaymentDetailsRequest.prototype['id'] = undefined;

/**
 * @member {String} paymentToken
 */
SecurePaymentDetailsRequest.prototype['paymentToken'] = undefined;

/**
 * @member {module:model/SecurePaymentDetailsRequest.TypeEnum} type
 */
SecurePaymentDetailsRequest.prototype['type'] = undefined;

/**
 * @member {Boolean} includeRefundTransactions
 */
SecurePaymentDetailsRequest.prototype['includeRefundTransactions'] = undefined;





/**
 * Allowed values for the <code>type</code> property.
 * @enum {String}
 * @readonly
 */
SecurePaymentDetailsRequest['TypeEnum'] = {

    /**
     * value: "AnytimePayment"
     * @const
     */
    "AnytimePayment": "AnytimePayment",

    /**
     * value: "PaymentIntent"
     * @const
     */
    "PaymentIntent": "PaymentIntent",

    /**
     * value: "PaymentLink"
     * @const
     */
    "PaymentLink": "PaymentLink",

    /**
     * value: "OpenPayment"
     * @const
     */
    "OpenPayment": "OpenPayment"
};



export default SecurePaymentDetailsRequest;

