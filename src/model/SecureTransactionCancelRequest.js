/**
 * Secure API
 * Secure API
 *
 * The version of the OpenAPI document: 2.3
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 *
 */

import ApiClient from '../ApiClient';

/**
 * The SecureTransactionCancelRequest model module.
 * @module model/SecureTransactionCancelRequest
 * @version 2.3
 */
class SecureTransactionCancelRequest {
    /**
     * Constructs a new <code>SecureTransactionCancelRequest</code>.
     * @alias module:model/SecureTransactionCancelRequest
     * @param transactionId {String} 
     * @param voidReason {String} 
     */
    constructor(transactionId, voidReason) { 
        
        SecureTransactionCancelRequest.initialize(this, transactionId, voidReason);
    }

    /**
     * Initializes the fields of this object.
     * This method is used by the constructors of any subclasses, in order to implement multiple inheritance (mix-ins).
     * Only for internal use.
     */
    static initialize(obj, transactionId, voidReason) { 
        obj['transactionId'] = transactionId;
        obj['voidReason'] = voidReason;
    }

    /**
     * Constructs a <code>SecureTransactionCancelRequest</code> from a plain JavaScript object, optionally creating a new instance.
     * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @param {module:model/SecureTransactionCancelRequest} obj Optional instance to populate.
     * @return {module:model/SecureTransactionCancelRequest} The populated <code>SecureTransactionCancelRequest</code> instance.
     */
    static constructFromObject(data, obj) {
        if (data) {
            obj = obj || new SecureTransactionCancelRequest();

            if (data.hasOwnProperty('transactionId')) {
                obj['transactionId'] = ApiClient.convertToType(data['transactionId'], 'String');
            }
            if (data.hasOwnProperty('voidReason')) {
                obj['voidReason'] = ApiClient.convertToType(data['voidReason'], 'String');
            }
        }
        return obj;
    }

    /**
     * Validates the JSON data with respect to <code>SecureTransactionCancelRequest</code>.
     * @param {Object} data The plain JavaScript object bearing properties of interest.
     * @return {boolean} to indicate whether the JSON data is valid with respect to <code>SecureTransactionCancelRequest</code>.
     */
    static validateJSON(data) {
        // check to make sure all required properties are present in the JSON string
        for (const property of SecureTransactionCancelRequest.RequiredProperties) {
            if (!data.hasOwnProperty(property)) {
                throw new Error("The required field `" + property + "` is not found in the JSON data: " + JSON.stringify(data));
            }
        }
        // ensure the json data is a string
        if (data['transactionId'] && !(typeof data['transactionId'] === 'string' || data['transactionId'] instanceof String)) {
            throw new Error("Expected the field `transactionId` to be a primitive type in the JSON string but got " + data['transactionId']);
        }
        // ensure the json data is a string
        if (data['voidReason'] && !(typeof data['voidReason'] === 'string' || data['voidReason'] instanceof String)) {
            throw new Error("Expected the field `voidReason` to be a primitive type in the JSON string but got " + data['voidReason']);
        }

        return true;
    }


}

SecureTransactionCancelRequest.RequiredProperties = ["transactionId", "voidReason"];

/**
 * @member {String} transactionId
 */
SecureTransactionCancelRequest.prototype['transactionId'] = undefined;

/**
 * @member {String} voidReason
 */
SecureTransactionCancelRequest.prototype['voidReason'] = undefined;






export default SecureTransactionCancelRequest;

